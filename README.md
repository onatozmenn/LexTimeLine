
  # LexTimeLine

  This is a code bundle for LexTimeLine. The original project is available at https://www.figma.com/design/7kGui00pOzMbpXMYEplaLn/LexTimeLine.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  