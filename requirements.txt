# =============================================================================
# LexTimeline - Python Dependencies
# =============================================================================
# Install all dependencies with:
#   pip install -r requirements.txt
#
# For a reproducible environment, use:
#   pip install -r requirements.txt --require-hashes
# (generate hashes with pip-compile --generate-hashes)
# =============================================================================

# --- Web Framework -----------------------------------------------------------
fastapi>=0.111.0,<0.200.0        # High-performance async API framework
uvicorn[standard]>=0.29.0,<1.0.0 # ASGI server (includes uvloop + httptools)
aiofiles>=23.2.0,<25.0.0         # Required for FastAPI StaticFiles / FileResponse

# --- PDF Parsing -------------------------------------------------------------
PyMuPDF>=1.24.0,<2.0.0           # High-performance PDF text extraction (fitz)

# --- AI / LLM ----------------------------------------------------------------
openai>=1.30.0,<2.0.0            # Official OpenAI SDK with async support

# --- Data Validation ---------------------------------------------------------
pydantic>=2.7.0,<3.0.0           # Data validation and schema definition

# --- Environment & Config ----------------------------------------------------
python-dotenv>=1.0.0,<2.0.0      # .env file loader for local development

# --- HTTP (FastAPI dependency, explicit for clarity) -------------------------
httpx>=0.27.0,<1.0.0             # Async HTTP client used by openai SDK
python-multipart>=0.0.9,<1.0.0   # Required for FastAPI file uploads (UploadFile)

# --- Dev / Testing (optional, comment out for production) -------------------
pytest>=8.2.0,<9.0.0             # Test runner
pytest-asyncio>=0.23.0,<1.0.0    # Async test support for FastAPI
httpx>=0.27.0,<1.0.0             # Also used as the FastAPI TestClient backend
